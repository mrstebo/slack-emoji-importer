default: &default
  adapter: postgresql
  pool: 5
  username: postgres
  host: db

development:
  <<: *default
  database: slack_emoji_importer_development

test:
  <<: *default
  database: slack_emoji_importer_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
